# crates/tools/Cargo.toml
[package]
name = "toolbox-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
toolbox-core = { path = "../core" }
serde_json = "1"
base64 = "0.22"
chrono = { version = "0.4", features = ["clock", "std"] }
ctor = "0.5"
